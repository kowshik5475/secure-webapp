# Core web framework
flask>=3.1.0

# Password hashing — bcrypt is the OWASP-recommended algorithm
bcrypt>=5.0.0

# JWT generation and validation
PyJWT>=2.8.0

# Rate limiting (OWASP A07)
Flask-Limiter>=4.0.0

# File type detection by magic bytes (OWASP A04)
# Pure Python — no libmagic system dependency required
filetype>=1.2.0
